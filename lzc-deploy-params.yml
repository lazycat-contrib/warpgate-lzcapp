params:
  - id: adminpassword
    type: secret
    name: "Warpgate Admin Password"
    description: "The password for Warpgate admin user"
    default_value: "admin"

locales:
  zh:
    adminpassword:
      name: "Warpgate管理员密码"
      description: "您要使用的WarpGate管理员admin的密码"
